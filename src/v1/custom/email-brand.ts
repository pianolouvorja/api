/**
 * EMAIL_BRAND_TOKENS — design tokens da marca para e-mails da API.
 * PORTADO do piano-site (server/utils/email-brand.ts) — manter os dois
 * em sincronia. NUNCA usar cores hardcoded em templates.
 */
export const EMAIL_BRAND_TOKENS = Object.freeze({
  cyan: "#00c1e6",
  cyanLight: "#5dd9f0",
  yellow: "#fcce02",
  yellowDark: "#e0b800",
  dark: "#0a1733",
  slate: "#1e2a45",
  slateLight: "#475569",
  muted: "#64748b",
  text: "#e2e8f0",
  border: "#1e293b",
});
